#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: the character to check
 * Return:check if and return 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
